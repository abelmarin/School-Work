#include <stdio.h>

// change the parameters
int sum_array() {
  return 0;
}

int sum_array_ptr() {
  return 0;
}

int main() {
  int sum = 0;
  int numbers[] = {3, 1, 2, 3, 6, 2, 6, 1, 0, 6};
  // sum = sum_array(???);
  printf("The sum is %d\n", sum);
  // sum = sum_array_ptr(???);
  printf("The sum is %d\n", sum);
}
